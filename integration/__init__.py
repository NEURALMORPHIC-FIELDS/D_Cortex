# D_Cortex integration spine: LLM extractor (Qwen) -> sealed symbolic organ
# (DeterministicObjectBank + CommitArbiterPas7a) -> vetoed verbalizer (Qwen).
# New code only. The sealed v15.7a substrate and dcortex/ are never edited; the
# substrate is loaded in-memory from steps/13_v15_7a_consolidation/code.py, which
# stays byte-identical on disk.
